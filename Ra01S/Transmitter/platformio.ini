; PlatformIO Project Configuration File
; LoRa Transmitter for ESP32 with RA-01S/SH module

[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 115200
upload_speed = 921600

; Library dependencies
lib_deps =
    ; No external dependencies - Ra01S library included locally

; Build flags (uncomment to enable EBYTE module support)
; build_flags = -D USE_EBYTE
